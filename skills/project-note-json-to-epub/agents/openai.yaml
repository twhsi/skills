interface:
  display_name: "專案筆記Json轉Epub/PDF"
  short_description: "目錄索引卡交叉連結轉EPUB/PDF"
  default_prompt: "Use $project-note-json-to-epub to convert this chapter/project JSON into an EPUB and optional Kindle-clickable PDF with directory cards, keyword index cards, backlinks, chapter split plan, and full link validation."

policy:
  allow_implicit_invocation: true
