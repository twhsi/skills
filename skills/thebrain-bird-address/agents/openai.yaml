interface:
  display_name: "BIRD 2.0 拆書分析"
  short_description: "把書稿拆成 BIRD Excel、TheBrain 與 Roam JSON"
  default_prompt: "Use $thebrain-bird-address to deconstruct this manuscript into a validated BIRD 2.0 knowledge address system and export Roam Research import JSON when requested."
